# W3 开发任务拆分（阅读 + 管线里程碑）

> 对齐：《技术方案文档.md》v1.0 — §5 异步管线 / §7.1 路由 / §7.2 BFF 契约 / §7.4 Range 代理 / §7.6 SSRF / §8.1 组件树 / §13 里程碑（W3–4 行）。
> 范围：W3–4 = PDF.js 阅读页（含 Range 代理）+ Worker(download/parse) + 队列 + 目录跳转（书签+反推兜底）。
> AI 核心（指标抽取/摘要/问答/引用定位）在 **W5–6**，本里程碑仅留 AI 面板外壳 + 契约占位，不接真实模型。

---

## W3 — 阅读页 + 管线

| # | 任务 | 关键交付 | 验收（可独立判定） |
| --- | --- | --- | --- |
| W3-1 | PDF.js 阅读页骨架 | `app/reader/[id]/page.tsx`(SSR 取 doc 详情+toc)；`ReaderPage`(三栏响应式)；`PdfViewer`(pdfjs-dist 自托管 worker、按页懒加载 canvas+文本层、暴露 `scrollToPage`)；`TocPanel`(点击跳页,键盘可达)；`AiPanel`(WIP 外壳)；pdfjs-dist 依赖 + worker 拷 public | `next build` 通过；阅读页 SSR 渲染；三栏布局 + 移动端单栏 |
| W3-2 | Range 代理 BFF `GET /api/disclosures/[id]/pdf` | 源选择(主源→备用源)；SSRF 防护(域名白名单/禁内网/限大小超时)；Range 透传(206+Accept-Ranges+Content-Range)；对象存储签名 URL 优先 → 流式 → 整文件降级；dev 本地 fixture 回退便于验证 | curl 带 `Range` 返回 `206`+正确 `Content-Range`；非 PDF/内网 URL 被拒 |
| W3-3 | TOC 面板 + 目录跳转 | 书签解析(parser 产出 toc JSON) + 反推兜底(按章节标题正则)；点击/键盘跳页调 `PdfViewer.scrollToPage`；段落高亮脚手架(命中子串绘制半透明矩形) | TOC 渲染且点击跳页；键盘可达；高亮坐标来自文本层 |
| W3-4 | Worker 管线 MVP | `downloader`(巨潮主源+东财兜底, SSRF 同源防护)；`parser`(PyMuPDF 分页文本+目录)；RQ 队列 + 内部 enqueue 端点(`POST /internal/enqueue`, 仅监听内网)；幂等键 `report_id:stage` | 本地用种子 PDF 跑 download→parse→入库 chunks/toc；产物写入 document_chunks |
| W3-5 | 联调冒烟 | 串 W3-1~W3-4；假 TOC/假 PDF 验证阅读页+代理+跳页；记录已知问题；Lighthouse 基线（待浏览器环境补） | 冒烟清单全过；记录基线分与已知问题 |

---

## 依赖与执行顺序
```
W3-1 ─► W3-2 ─► W3-3 ─┐
                       ├─► W3-5 (冒烟)
W3-4 ──────────────────┘
```
- W3-2 依赖 W3-1（同一阅读页前后端）。
- W3-3 依赖 W3-1（PdfViewer.scrollToPage）。
- W3-4 独立，但需 `requirements-parse.txt` 重型依赖（pymupdf/psycopg2 等），沙箱内安装慢 → 建议预缓存离线 wheel 或延后到本地环境跑。
- W3-5 依赖全部。

## 本里程碑明确不做（防范围蔓延）
- ❌ 指标抽取 / 摘要 / RAG 问答 / 引用定位真实模型调用（W5–6）
- ❌ 三大报表可视化、段落级高亮完整实现（P1，W3-3 仅脚手架）
- ❌ 准确率 CI gates、并发压测（W7–10）
- ❌ 招股书全量接入（P1）

## 待确认（开工前）
- 阅读页真实 PDF 来源：dev 用本地 fixture 回退验证；生产走 MinIO 签名 URL / 巨潮直链（§7.4）。
- `disclosures.toc` 新增 JSONB 列（parser 写入，BFF 读取）；以新迁移 `0003_disclosure_toc.sql` 追加，不改动冻结基线。
- Worker 重型依赖安装策略：沙箱网络受限，建议本地环境跑 W3-4 真实验证。
