# W5-6 AI 核心 · 任务拆分

> 依赖：W1-W2（脚手架/DB/BFF）、W3（阅读器+解析+管线）。本里程碑实现 AI 能力：指标抽取、向量化、RAG 问答、引用定位。
> 沙箱限制：无 Postgres/Redis/外网，无 torch/sentence-transformers/LLM。故本环境只跑**纯逻辑单测 + 桩**，重依赖与 DB/浏览器终验留本地 `docker compose` + `python -m worker.verify_w5_6`。

## 子任务

| 任务 | 内容 | 验收 | 沙箱可验 |
| --- | --- | --- | --- |
| W5-6a | 指标抽取 `metrics.py`：营收/归母净利润/经营现金流，固化预研 4 坑规则，累计口径(year_to_date) + 同比(yoy) + caliber；`db.write_metrics` 幂等写 | 茅台 2022 三项值与实盘一致；单测覆盖 4 坑 | ✅ 纯逻辑单测 |
| W5-6b | 向量化 `embed.py`：bge-small-zh(512 维) 惰性加载，读 chunks→embed→写 `document_chunks.embedding`；pipeline 分发 embed/metrics | 单测用注入式 embedder 验证编排与幂等写 | ✅ 注入桩单测 |
| W5-6c | RAG 检索 `retrieval.py`：pgvector 余弦 top-k + 可选 BM25 RRF 重排；引用强制校验 | 检索/重排纯逻辑单测(mocked DB) | ✅ mocked 单测 |
| W5-6d | BFF 路由：`/metrics`、`/summary`(POST 预计算缓存)、`/chat`(POST RAG)；AI 网关客户端(OpenAI 兼容, 提示注入防护) | 路由存在 + 契约与 §7.2 对齐；逻辑单测 | 部分(契约/纯逻辑) |
| W5-6e | 前端 AiPanel：`<SummaryCard/>`(引用 chip)、`<MetricCard/>×3`(↑↓标红)、`<ChatBox/>`(引用 chip→跳页高亮) | tsc 通过；复用 W3-3 高亮脚手架 | ✅ tsc |
| W5-6f | 构建验证：`tsc` + `py_compile` + 本地 `verify_w5_6.py` 终验 | 全绿 | ✅ 编译/单测 |

## 关键设计点（对齐技术方案 §6.4/§6.5/§7.2/§7.3/§8.2/§9）
- **指标 4 坑**：① 关键词容忍词内空白 ② 数值保留原始换行不全局去空白 ③ `parse_number` 仅去千分位逗号保留小数点 ④ 多表同名命中即停取最先主表值。
- **口径**：A 股季报为累计口径；MVP `value_scope=year_to_date`、`qoq` 置空、yoy 仅在主表同页含上年同期数时计算。
- **embedding**：MVP 固定 bge-small-zh(512 维)，与 `document_chunks.embedding vector(512)` 及 ivfflat 索引一致；模型惰性加载，嵌入调用可注入（便于单测）。
- **引用强约束**：问答每引用 `[page, text]` 必须命中检索片段集合；检索片段视为不可信数据，剥离指令式文本（提示注入防护）。
- **准确性 gate（MVP 阻断）**：指标抽取误差 ≤1% & 覆盖率 ≥95%；页码引用准确率 ≥95%；答案+引用正确率 ≥85%。

## 已知限制（与 W3 一致）
- 重依赖(torch/sentence-transformers/LLM)与 DB/浏览器终验在本地环境完成。
- `next build` 末段清理仍受 sandbox safe-delete 守卫拦截——用 `tsc --noEmit` 验证前端类型。
