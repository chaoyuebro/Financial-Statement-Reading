# 当前开发进展（截至 2026-07-19）

> 当前代码已完成 W1–W6 的主要功能链路，进入“真实数据接入、准确率验收与体验打磨”阶段。

## 已完成并验证

- **W1–W2 基建与首页**
  - Next.js + PostgreSQL/pgvector + Redis/RQ + MinIO + Python Worker 五服务可运行。
  - 首页支持年报、半年报、季报筛选，以及公司名称、简称和股票代码查询。
  - `600519.SH` 浏览器实测能正确归一化并返回贵州茅台报告。
- **W3–W4 阅读与解析**
  - 首页报告卡片已接通 `/reader/[id]` 阅读页。
  - PDF.js 画布、文本层、Range 代理、按页懒加载、页内查找和高亮可用。
  - Docker 开发环境通过 `FR_PDF_DEV_FIXTURE` 使用本地三页 PDF 完成浏览器实渲染验证。
  - Worker 的 download → parse、目录写入、分块和状态机已完成实栈验证。
- **W5–W6 AI 核心**
  - 营业收入、归母净利润、经营现金流三项指标卡片可用。
  - 摘要接口和问答接口可用，引用可点击跳页。
  - 无 LLM 时自动降级为抽取式回答。
  - `verify_w5_6.py` 在 Docker 实栈通过：Worker 鉴权、参数校验、指标、摘要、问答全部 PASS。
- **工程质量**
  - `npm run build` 通过。
  - 根目录新增统一 `npm test`，覆盖 TypeScript 检查与 AI 纯逻辑单测（8/8）。
  - Worker Python 文件编译检查 9/9。

## 本轮修复

1. 修复首页报告卡片只高亮、不进入阅读页的问题。
2. 修复 W5–W6 种子脚本错误的 `canonical_key='annual:2022'`，统一为
   `600519:annual:2022` 并复用已有规范报告，避免重复首页记录。
3. 清理开发库中重复的贵州茅台 2022 年报；当前重复报告分组为 0。
4. 修复 `_check_reports.py` 错误地用 `canonical_key` 关联公司表的问题。
5. 修复 AI 单测导致 TypeScript 类型检查失败的问题，并补统一测试命令。
6. 开发 Docker 环境接入安全受控的 PDF fixture，真实浏览器验证 PDF 画布、文本层与高亮。

## 当前未完成 / 风险

- **V1 数据稳定性**：巨潮连续 3 天抓取成功率尚未验收。
- **V2 极端 PDF**：500+ 页与扫描件 OCR 尚未正式验收。
- **V4 指标准确率**：当前仅演示数据和少量样本，尚未在 ≥30 份分层测试集达到覆盖率门槛。
- **V5 AI 准确率**：
  - 已接入 MiniMax-M3，并用 FastEmbed `BAAI/bge-small-zh-v1.5` 生成 512 维向量。
  - 真实茅台 2025 年报 270/270 个文本块已完成向量化，向量 + 全文 BM25 + RRF 混合检索可用。
  - 答案 ≥85%、页码引用 ≥95% 尚未在正式 QA 测试集完成评测。
- **真实三类报告**：
  - 贵州茅台 2025 年报、2025 半年报、2025 三季报均已完成解析、三项指标抽取、
    512 维 Embedding、MinIO 缓存、PDF Range 阅读与 MiniMax-M3 问答。
  - 三季报指标已按“年初至报告期末”累计口径抽取，避免误用单季度值。
- **真实数据**：开发列表大部分 PDF 地址仍为占位符；正式体验需接入真实 CNINFO 抓取与解析结果。
- **体验验收**：Lighthouse、移动端抽屉、键盘全流程和段落级高亮仍需继续打磨。

## 下一步优先级

1. 建立 ≥50 条 QA 测试集并运行 V5 准确率 gate。
2. 扩展到 ≥30 份跨公司、跨报告类型的指标测试集。
3. 补 500+ 页和扫描件 OCR 测试。
4. 跑 Lighthouse 与移动端/键盘可访问性检查。
