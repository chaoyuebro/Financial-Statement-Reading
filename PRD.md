# 财报 / 招股书辅助阅读工具 · 产品需求文档（PRD）

| 字段 | 内容 |
| --- | --- |
| 文档名称 | 财报 / 招股书辅助阅读工具 PRD |
| 版本 | v0.4（评审收缩 + 预研 + 技术基线） |
| 更新日期 | 2026-07-18 |
| 状态 | 已评审 / 开发基线 |
| 创建日期 | 2026-07-18 |
| 负责人 | 产品 + 前端开发 |
| 目标读者 | 产品、设计、前端、后端、AI 工程 |

> 说明：本文档聚焦"做什么"和"为什么做"，技术实现细节仅给出方向性建议（偏前端视角），后续需拆分为更细的技术方案与开发任务。

---

## 1. 产品背景与愿景

### 1.1 背景
- **信息披露海量且专业**：A 股上市公司每年需披露年报、半年报、季报，IPO 企业披露招股说明书（常 400–1000 页）。这些信息集中在巨潮资讯网（CNINFO）等官方平台。
- **阅读门槛高**：财报与招股书充满会计术语、合并报表、附注与行业黑话，普通个人投资者（"小白"）难以快速抓住重点、判断投资价值。
- **现有工具割裂**：巨潮资讯适合"查"，但不适合"读"；同花顺 i 问财、雪球侧重行情与讨论，缺乏对单份文档的结构化解读与对话式问答。

### 1.2 产品愿景
> 做一个"像巨潮资讯一样能看全量披露，又像私人投顾一样能陪你读懂"的阅读工具。

核心一句话：**聚合最新财报与招股书，提供结构化阅读 + 对话式 AI 辅助，让投资新手 10 分钟内读懂一份报告的关键信息。**

### 1.3 成功指标（北极星）
- 用户打开一份报告后，**3 分钟内**看到结构化摘要与关键指标（首屏价值密度）。
- AI 问答准确率（人工抽检）≥ 85%，引用须可回溯到原文（**最终目标：段落级回溯；MVP 仅验收页码级引用，段落高亮属 P1**，见 4.3.3 / 12）。
- 周活留存（新手用户）≥ 30%。
- 首页信息披露覆盖度与官方平台**同步延迟 < 1 小时**。

---

## 2. 目标用户与场景

| 角色 | 特征 | 核心诉求 | 典型场景 |
| --- | --- | --- | --- |
| **投资新手 / 小白** | 无财务基础，想了解某公司是否赚钱、值不值得关注 | 用大白话讲清楚、术语有人解释、能问答 | "茅台去年赚了多少？""招股书里说的募投项目是啥？" |
| **个人投资者** | 有一定经验，关注关键指标与风险 | 快速比对、风险提示、行业对比 | "对比一下宁德时代近三年毛利率" |
| **研究 / 财经自媒体** | 需提炼观点、找数据支撑 | 结构化抽取、引用定位、导出 | 写文章需要某指标逐年数据 |
| **学生 / 从业者** | 学习财报阅读 | 带教式解读、术语词典 | 学习三大报表勾稽关系 |

**本版（MVP）优先服务"投资新手 / 小白"**，其余角色的功能作为后续扩展。

---

## 3. 竞品分析（简版）

| 产品 | 优势 | 不足（我们的机会） |
| --- | --- | --- |
| 巨潮资讯 | 官方、全量、权威 | 体验旧、无解读、无 AI、PDF 阅读差 |
| 同花顺 i 问财 | 自然语言问数据强 | 偏行情，单文档深度解读弱 |
| 雪球 / 东方财富 | 社区活跃、讨论多 | 信息噪音大，缺乏原文回溯 |
| 招股书垂直工具（如部分小程序） | 聚焦解读 | 覆盖窄、更新慢、交互简陋 |

**差异化定位**：官方级覆盖 + 单文档深度结构化解读 + 可回溯的对话式 AI。

---

## 4. 产品功能需求

### 4.1 信息披露列表页（首页，类巨潮资讯）

**目标**：用户一进来就能看到"最新发生了什么披露"。

#### 4.1.1 功能列表
- **F-101 披露列表**：分页 / 无限滚动展示最新财报与招股书。
  - 字段：公司名称、股票代码、报告类型（年报 / 半年报 / 季报 / 招股书）、报告期、披露日期、交易所（沪 / 深 / 北）、文件格式（PDF）。
  - 默认排序：披露日期倒序。
- **F-102 筛选与搜索**
  - 按类型筛选（财报 / 招股书 / 快报 / 预告）。
  - 按交易所筛选（上交所 / 深交所 / 北交所）。
  - 关键词搜索：支持按**公司名称**或**股票代码**查询。
  - 公司名称支持完整名称、简称及模糊匹配；股票代码支持精确匹配，并兼容用户输入交易所后缀（如 `600519`、`600519.SH`）。
  - 无匹配结果时展示明确的空状态，并保留当前搜索词与筛选条件。
  - 时间范围筛选（近 7 天 / 30 天 / 自定义）。
- **F-103 排序**：按披露时间、按公司热度（可后续接入热度）。
- **F-104 报告入口**：点击任意条目 → 进入阅读页（见 4.2）。
- **F-105 关注 / 订阅（可选，后续）**：关注某公司后，其新披露置顶并推送。

#### 4.1.2 页面结构（信息架构）
```
首页 (Home)
├─ 顶部：搜索框 + 类型 Tab（全部/财报/招股书/快报/预告）
├─ 筛选条：交易所、时间范围、排序
├─ 列表：卡片 / 行式，每条可点
└─ 底部分页 / 加载更多
```

#### 4.1.3 关键交互
- 首屏 LCP < 2.5s，列表虚拟滚动（数据量大时）。
- 列表项 hover 显示"快速摘要"气泡（可选）。
- 支持键盘上下导航 + 回车打开（无障碍）。

---

### 4.2 报告阅读页

**目标**：在浏览器内流畅阅读 PDF，并随时调用 AI。

#### 4.2.1 功能列表
- **F-201 PDF 在线阅读**
  - 基于 PDF.js 渲染，支持分页 / 连续滚动、缩放、目录（书签）跳转、文本复制。
  - 大文件懒加载（按页 / 按范围请求），避免一次性加载几百页。
- **F-202 原文定位**
  - AI 回答中引用的内容可点击，跳转到 PDF 对应**页码**（**MVP 验收页码级定位**）；段落级精确高亮为产品最终目标，属 P1（见 4.3.3 / 12），避免验收人员据顶层总要求否决 MVP。
- **F-203 结构化信息栏（侧边 / 浮层）**
  - 自动抽取关键指标卡片（见 4.3）。
  - 文档大纲（章节导航）。
- **F-204 阅读进度 / 书签**（本地存储，登录后可云同步）。
- **F-205 文档元信息**：公司、报告期、审计意见、币种等基础信息头。

#### 4.2.2 页面结构
```
阅读页 (Reader)
├─ 顶栏：公司名 + 报告类型 + 期 + 下载/分享 + 返回
├─ 左：目录 / 大纲（可折叠）
├─ 中：PDF 阅读区（主区域）
└─ 右：AI 辅助面板（默认折叠，可展开）
```

---

### 4.3 AI 辅助阅读模块（核心差异化）

**目标**：让小白"读得懂、问得出、信得过"。

#### 4.3.1 小白模式 vs 专家模式
- **小白模式**：用通俗语言、类比、短句；主动解释术语；结论先行。
- **专家模式**：保留专业表述，给出完整勾稽关系与口径说明。

#### 4.3.2 功能列表
- **F-301 一键智能摘要**
  - 生成 5–8 条要点：营收、净利润、增长、核心业务变化、主要风险。
  - 输出结构化卡片，每条带"原文出处"链接（跳转 F-202）。
- **F-302 关键财务指标卡片**
  - 自动抽取：营业收入、归母净利润、毛利率、净利率、资产负债率、ROE、经营现金流、研发投入占比等。
  - 展示同比 / 环比变化（↑/↓ + 百分比），异常值标红提示。
  - 招股书场景：募资总额、发行价区间、市盈率、募投项目列表。
- **F-303 三大报表可视化**
  - 资产负债表 / 利润表 / 现金流量表：以图表呈现主要科目趋势（近 3–5 期）。
  - 科目可点击查看定义（接术语词典）。
- **F-304 术语词典 / 划词解释**
  - 用户选中文本 → 弹出"这是什么意思"解释。
  - 内置财务术语库（如：商誉、递延所得税、经营性现金流）。
- **F-305 对话式问答（RAG）**
  - 基于当前文档的检索增强问答："这家公司主要收入来源是什么？""应收账款为什么大幅增加？"
  - 必须附**原文引用**（页码 + 片段），杜绝"一本正经胡说"。
  - 支持追问与上下文记忆。
- **F-306 风险提示摘要**
  - 从"风险因素""管理层讨论"章节提取主要风险，分级（高 / 中 / 低）。
- **F-307 对比分析（后续）**
  - 同公司跨期对比、同业对比。

#### 4.3.3 AI 输出约束（重要）
- **可回溯**：所有结论须能定位到原文（**MVP 验收页码级引用；段落级高亮回溯为产品最终目标，属 P1**）。
- **免责声明**：AI 内容仅供参考，不构成投资建议（固定提示条）。
- **不确定即说不确定**：文档未提及的内容，明确回复"报告中未披露"。

---

### 4.4 账户与个性化（后续，非 MVP）
- 登录（手机号 / 微信）。
- 关注公司、阅读历史、收藏、AI 对话记录云同步。
- 推送：关注公司新披露提醒。

---

## 5. 信息架构与核心流程

### 5.1 核心用户流程
```
[首页列表] --点击--> [阅读页] --展开AI面板--> [摘要/问答]
                                   ↑                |
                                   |--点击引用--> [原文高亮定位]
[搜索/筛选] --结果--> [首页列表]
```

### 5.2 状态流转（报告数据）
`抓取中 → 已解析文本 → AI 指标抽取完成 → 可阅读+可问答`

---

## 6. 关键页面与交互设计（UX 方向）

> 视觉风格建议：专业、克制、信息密度高但不拥挤；主色金融蓝 + 中性灰，风险提示用暖色。参考巨潮资讯的信息层级，但大幅优化阅读体验。

| 页面 | 关键设计点 |
| --- | --- |
| 首页 | 信息流布局，类型 Tab + 筛选条固定；列表项突出"公司 + 类型 + 日期" |
| 阅读页 | 三栏（目录 / PDF / AI），移动端 AI 面板改为底部抽屉 |
| AI 面板 | 顶部"摘要"一键生成；下方对话区；每条回答带引用 chip |
| 指标卡片 | 数字大、变化箭头醒目、异常标红；点击展开口径说明 |

**响应式**：桌面三栏；平板两栏（AI 收起）；移动端单栏 + 底部 AI 抽屉。
**无障碍**：语义化 HTML、ARIA、键盘可达、≥ 4.5:1 对比度、尊重 `prefers-reduced-motion`。

---

## 7. 数据需求与来源

### 7.1 数据需求
| 数据 | 用途 | 说明 |
| --- | --- | --- |
| 披露列表元数据 | 首页列表 | 公司、代码、类型、日期、PDF URL |
| PDF 原文 | 阅读 + 解析 | 官方 PDF，需后端代理解决跨域 / 防盗链 |
| 文本与表格 | AI 抽取 / 问答 | 解析文本、表格、目录 |
| AI 能力 | 摘要 / 问答 | LLM + 向量检索（RAG） |

### 7.2 数据管道（建议）
```
官方披露 → 定时抓取元数据 → 入库
                  ↓
          触发 PDF 下载 + 解析（文本/表格/目录）
                  ↓
          切片 + Embedding 入库（向量库）
                  ↓
          指标抽取（LLM/规则）→ 指标卡片缓存
                  ↓
          前端可阅读 + 可问答
```

### 7.3 数据合规（闭环待办，上线前必清）
- 仅聚合**已公开披露**的官方信息，注明来源与原文链接。
- 不存储、不转售原始 PDF 之外的衍生数据用于非法用途。
- AI 输出附"仅供参考，非投资建议"声明。
- ⚠️ "公开可访问" ≠ 允许持续抓取 / 缓存 / 商业化展示；免责声明 ≠ 数据授权。上线前必须确认以下清单：
  - **接口条款与抓取频率**：巨潮 / 东财的 API ToS、robots、限频与频率上限。
  - **PDF 缓存策略**：能否长期保存副本？还是仅按需代理、不落盘？
  - **商业化边界**：东方财富数据能否作为商业产品数据源（建议其仅用于原型 / 兜底，正式源用巨潮）。
  - **展示方式**：仅展示官方链接 + 按需代理，还是保存副本供离线阅读？
  - **失效与更新**：来源失效、内容更新、公告撤回（撤回公告需下架并提示）的处理流程。
  - **法务复核**：正式上线前由法务确认授权与合规边界。

### 7.4 数据源方案对比（已选：方案 D 混合）⚠️
目标：拿到"披露列表 + PDF 原文"。以下 4 个方案均可独立落地，推荐 **D（混合）**。

**方案 A：巨潮资讯官方接口 + 后端代理（权威主源）**
- 列表：`POST http://www.cninfo.com.cn/new/hisAnnouncement/query`（form：`pageNum`,`pageSize`,`column=szse|shse|bse`,`tabName=fulltext`,`stock=代码,orgId`（如 `600519,gssh0600519`）,`seDate` 起止日期,`category`=报告类型编码 如 `category_ndbg_szsh` 年报 / `category_sf_szsh` 招股书）。
- PDF：真实下载地址 `http://static.cninfo.com.cn/<adjunctUrl>`（取自查询响应中的 `adjunctUrl` 字段，由后端 Worker 拼接）。⚠️ 旧地址 `http://www.cninfo.com.cn/new/disclosure/download?bulletinId=...` 实测已 **404 失效，不可再用**。
- ✅ 权威、沪深北全覆盖、免费、字段全（含招股书）。
- ⚠️ 有反爬（需带 UA / cookie、限频），接口可能微调；必须后端代理。

**方案 B：东方财富数据中心 API（np-anotice）**
- `https://np-anotice-stock.eastmoney.com/api/security/ann?sr=-1&page_size=10&page_index=1&ann_type=2&stock_list=000001`
- 返回公告元数据（标题 / 日期 / `art_code` 等），但**不含 PDF 直链字段**；PDF 真实地址规律为 `https://pdf.dfcfw.com/pdf/H2_<art_code>_1.pdf`（注意 `_1` 后缀，缺后缀返回 400）。⚠️ 实测 `stock_list` 按个股过滤在脚本侧被忽略（退化为全市场流），因此东方财富**不适合做"按个股列清单"的主源**，更适合拿 PDF 直链作兜底 / 校验。
- ✅ 接口稳定、结构化好、易解析、限频宽松。
- ⚠️ 非官方、口径偶有差异、极端情况滞后。适合快速 MVP / 兜底。

**方案 C：akshare（Python 数据层）**
- `stock_zh_a_disclosure_report_cninfo(stock='000001', report_type='年度报告', date='2023')` 等，聚合多源。
- ✅ 一行代码拿数据、最快搭原型、自带清洗。
- ⚠️ 需 Python 服务、实时性略差、依赖社区维护。

**方案 D（推荐落地）：CNINFO 主 + 东方财富兜底（混合）**
- 主源 CNINFO 保证权威与覆盖，东方财富作降级 / 校验，避免单点失效。
- ✅ 兼顾权威性与稳定性，工程上最稳。

> 落地口径（评审后 + 预研实测）：预研已用**巨潮 CNINFO（经 akshare 或复刻其请求）**按个股稳定拉到列表 + PDF（茅台 600519 年报 127 页、招股书 6 页均成功下载，见《技术预研计划》与下载样本）；**东方财富 np-anotice 的个股过滤在脚本侧被忽略**，仅适合拿 PDF 直链（`pdf.dfcfw.com/pdf/H2_<art_code>_1.pdf`）作兜底 / 校验。正式数据源以方案 A（巨潮）为主，最终落到 D。注意东方财富数据能否商用需走 7.3 合规清单确认。

---

## 8. 非功能性需求

| 类别 | 要求 |
| --- | --- |
| 性能 | 首页 LCP < 2.5s；PDF 首屏 < 3s（懒加载）；AI 摘要 < 8s；问答 < 10s（均为 **P95**，测试环境见 8.3） |
| 兼容性 | 最新 Chrome / Edge / Safari / Firefox；移动端 iOS 15+ / Android 10+ |
| 安全 | PDF 走后端代理防泄露；用户输入做注入过滤；API 限流防滥用 |
| 可观测 | 前端埋点（打开率、AI 使用率、引用点击率）；错误监控（零控制台错误为目标） |
| 合规 | 来源标注、免责声明、数据授权 |
| 数据质量 | 见 8.1 解析准确率基线 |

### 8.1 PDF 解析准确率基线（已定）
- 文本提取（原生数字 PDF）：字符准确率 ≥ 98%。
- 表格结构还原（数字 PDF，含合并报表复杂表）：≥ 85%（复杂合并表允许放宽至 75%）。
- 扫描件 OCR：文字准确率 ≥ 90%（需 OCR 兜底，解析失败降级为"仅阅读"）。
- 目录 / 书签解析成功率 ≥ 95%。

### 8.2 验收测量方法（必须配套，否则 8.1 只是目标不是门槛）
> 8.1 的数字是"目标"，要成为真正验收门槛，必须定义测量方法。预研阶段即建设测试集与脚本。

- **测试集**：≥ 30 份文档，覆盖年报、半年报、季报，并保留若干招股书用于后续能力预研；按"数字 PDF : 扫描 PDF ≈ 7:3"配比，覆盖沪 / 深 / 北各若干家。
- **文本准确率**：字符级准确率 = 1 − Levenshtein(提取文本, 标注文本) / len(标注文本)；以抽样页面人工标注为基准。
- **表格结构正确**：单元格级 F1（行 / 列 / 跨合并单元格还原），与标注表格比对；"结构正确"定义为 F1 ≥ 0.85。
- **财务指标验收**：营收 / 归母净利润 / 经营现金流等抽取值，与标注值误差 ≤ 1%（或绝对额小范围容忍）；同时记录口径（合并 / 母公司、归母 / 总和）。
- **页码引用准确率**：RAG 回答引用的页码，命中人工判定正确页码的比例 ≥ 95%（页码级）；段落级高亮在 P1 才计入。
- **AI 答案正确率**：基于 ≥ 50 条人工编写 QA 测试集（含"报告中未披露"负例），答案与引用均正确比例 ≥ 85%。
- **持续集成**：上述指标纳入 CI，每次解析 / 抽取改动后回归，防止退化。

### 8.3 性能验收测试环境（必须固定，否则指标不可比）
所有性能目标（§8 表、§12）统一以 **P95** 口径验收（非 P50），并在测试报告中固定以下环境，禁止"最优环境跑分"：

- **统计口径**：P95（95 分位延迟）；同时记录 P50 备查。
- **网络**：桌面端与移动端均模拟 **4G（下行 ~10 Mbps / RTT ~100ms）**；不采用局域网/光纤代表"达标"。
- **设备**：桌面 Chrome 最新稳定版 / 中端笔记本；移动 iOS 15+ / 中端 Android 10+。
- **PDF 样本**：覆盖 **50–400 页、3–30 MB**（年报/半年报/季报混合），不只用小文件。
- **缓存状态**：分别报告**冷缓存（首次访问）**与**热缓存（二次访问）**两项，不混用。
- **并发**：首页列表 ≥ 50 并发用户；阅读页 ≥ 20 并发；**AI 接口测试固定 ≥ 10 并发**（生产按成本上限弹性缩放，但验收须用固定 10 并发测量 P95，避免口径漂移）。
- **AI 模型**：固定测试所用模型与版本（如 `deepseek-chat@日期` / 本地 `qwen2.5-7b`），换模型须重测并标注。
- **报告模板**：测试报告逐条列出上述条件 + 实测 P95，作为 MVP 验收附件。

---

## 9. 技术架构建议（前端视角）

### 9.1 前端
- **框架与构建**：**Next.js 14.2（App Router）+ React 18.3 + TypeScript**（统一使用 Next.js 内置构建系统与 `next/dynamic` 动态导入，**不再使用 Vite**；版本锁定于团队验证过的稳定版并固定依赖，见技术方案文档第 1 节）。
- **PDF**：`pdfjs-dist` 自托管 worker，支持文本层与标注高亮。
- **状态**：Zustand / Context（轻量）；阅读进度与书签用 localStorage。
- **UI**：Tailwind CSS + 自建设计系统（指标卡片、引用 chip 等复用组件）。
- **图表**：轻量库（如 ECharts / Recharts）做报表趋势（P1 报表可视化）。
- **性能**：列表虚拟滚动、图片/PDF 懒加载（Range 流式代理）、Service Worker 缓存静态资源。

### 9.2 后端（概要）
- 四类职责：抓取服务、PDF 解析服务、向量检索服务、AI 网关（统一调用 LLM + 限流 + 审计）。
- 前端 API 契约（与 9.4 架构一致）：
  - `GET /disclosures`（列表，支持筛选分页）
  - `GET /disclosures/:id`（元信息 + 解析状态）
  - `GET /disclosures/:id/pdf`（代理 PDF 流）
  - `GET /disclosures/:id/metrics`（指标卡片）
  - `POST /disclosures/:id/summary`（生成摘要）
  - `POST /disclosures/:id/chat`（问答，带引用）

### 9.3 AI 工程要点（模型可配置）
- **模型可配置**：通过环境变量 / 配置文件选择供应商与模型，统一走 OpenAI 兼容协议，方便切换：
  - `AI_PROVIDER`（openai / hunyuan / qwen / deepseek / ollama ...）
  - `AI_BASE_URL`、`AI_MODEL`、`AI_API_KEY`
  - 支持混元、通义、DeepSeek、GPT 及本地 Ollama 等；可在设置页 / 配置文件切换，不需改代码。
- 文档切片 + 向量库（Milvus / pgvector / 轻量方案）。
- 检索 → 拼 prompt → LLM 生成 → 返回"答案 + 引用片段（page + text）"。
- 摘要与指标抽取可预计算并缓存，降低实时成本与延迟。

### 9.4 系统架构（已选：Next.js 主架构 + 第一版即引入 Python Worker）⚠️
> 评审修正关键点：原 PRD 写"Route Handler 内按需触发解析"仅适合小文件演示；**PDF 解析 / OCR / 表格提取 / Embedding 不作为长驻 Next.js 进程内的同步任务**，从第一版起走"任务队列 + Python Worker + 对象存储 / 向量库"的异步管线。仍称"Next.js 全栈主架构"，不必一开始拆成多微服务。

**目标形态（第一版即采用）**
```
Next.js (页面 + BFF + AI 网关)
   │  提交解析任务
   ▼
任务队列 (Redis / 轻量 MQ)
   │
   ▼
Python Worker (pdfplumber / OCR / 表格提取 / Embedding)
   │  写
   ▼
对象存储 (PDF/解析结果) + 向量库 (pgvector/Milvus) + 关系库 (元数据)
   │
   ▼
Next.js 读取结果 → 前端可阅读 + 可问答
```
- Next.js 只负责：SSR 页面、BFF、AI 网关（调用 LLM + 向量检索）、按需代理 PDF（是否长期缓存副本按 7.3 合规决定）。
- Python Worker 负责：下载、解析、OCR、表格、切片、Embedding、指标抽取（重 CPU / 内存、长耗时，独立伸缩）。
- 仍属"单一主架构"，Worker 可先与 Next.js 同仓、独立进程启动，不必拆微服务。

**备选方案（仅记录，不采用）**
- 方案 B 前后端分离 / 方案 C Serverless / 方案 D SPA+Node+Python 微服务：见 v0.2 历史；当前"方案 A 主架构 + Worker"已覆盖其优势且更简单。

---

## 10. 里程碑与优先级

### 0 阶段：技术预研（2 周，必做）
> 评审结论：当前排期可信度低（约 40%），先做 2 周预研再定正式工期最稳妥。预研必须验证 5 项：

1. **数据获取**：巨潮 / 东财接口可稳定拉到列表与 PDF（含反爬处理）。
2. **500 页 PDF 解析**：年报 / 招股书解析不超时、不 OOM，文本 + 目录可用。
3. **页码映射**：解析片段 → PDF 页码 → 前端高亮跳转的链路打通。
4. **关键指标抽取**：营收、归母净利润、经营现金流 三项可稳定抽取。
5. **带引用问答**：基于向量的 RAG 能返回"答案 + 正确页码引用"。

预研产出：可行性硬证据 + 正式工期估算（见 13）。

> **进度（截至 2026-07-18，晚）**：**预研 5 项均已完成"概念验证"，但均未达到《技术预研计划》原定 Go/No-Go 判据**。诚实状态如下（详见技术方案文档第 14 节对照表）：
> - **#1 数据获取** ⚠️ **部分通过，待连续运行验证**：已证明巨潮 CNINFO 可拉到个股年报 + 招股书真实 PDF（茅台年报 127 页、招股书 6 页；强一股份招股书 385 页 / 5.6 MB）；东财个股过滤不可用、旧 `download?bulletinId=` 404。但**仅下载过少量样本，未达到 V1"连续 3 天定时拉取成功率 ≥ 95%"** 的持续运行判据。
> - **#2 大 PDF 解析** ⚠️ **部分通过，待 500 页以上及扫描件验证**：385 页招股书 PyMuPDF 解析 0.5s / 峰值内存 1 MB，127 页年报 0.4s / 0.4 MB，无超时无 OOM；但 **385 页 < V2 要求的 500+ 页**，且**未验证扫描件 OCR**。"远超 500 页目标"表述不成立，已更正。
> - **#3 页码映射 + 目录** ✅ **页码映射通过，段落高亮未验收**：每个解析片段携带稳定页码（前端跳转链路打通）；目录质量因报告而异（招股书 `get_toc()` 640 条准确；年报书签常为 `OLE_LINK` 垃圾 → 正文反推兜底）。但**段落级精确高亮属 P1，本阶段未验收**。
> - **#4 指标抽取** ⚠️ **单份年报样本通过，尚未达到测试集覆盖率要求**：年报主表抽出营收（124,099,843,771.99）/ 归母（62,716,443,738.27）/ 经营现金流（36,698,595,830.03），数值与茅台 2022 实际一致且带页码；但**仅 1 份样本，未达到 V4"测试集覆盖率 ≥ 95%、误差 ≤ 1%"** 的验收口径。
> - **#5 带引用 RAG** ⚠️ **链路概念验证通过，准确率尚未验收**：零依赖 BM25 已能返回正确页码引用（"归母净利润"→第 5 页），证明链路打通；但**排序精度有限（"现金流为何减少"未排到最对页），且未完成 V5 要求的 Embedding + LLM + 测试集评测（答案 ≥ 85% / 引用 ≥ 95%）**。正式须用 pgvector 向量检索（技术方案 6.4 / 7.3）。
>
> **结论**：预研证明了"方向可行、无致命阻塞"，但**不能据此上调排期可信度**——剩余验收（连续拉取、500+ 页、扫描件、测试集、embedding + LLM 评测）必须在开发期 / W1–8 内补齐，否则排期仍按约 40–50% 可信度管理。
>
> **预研关键发现（工程化必须处理）**：
> 1. PDF 文本抽取的常见坑：① 指标标签被换行切断（`归属于上市公司股\n东的净利润`）→ 关键词匹配需容忍词内空白；② 相邻数字因去换行粘连 → 数值抽取须保留原始换行；③ `parse_number` 不能去掉小数点（否则数值 ×100）；④ 多表同名指标时"末页覆盖主表"→ 命中即停取最先出现的主表值。
> 2. 目录解析成功率（PRD 8.1"≥95%"）**必须以"书签 + 正文反推兜底"共同达标**来验收，单靠书签对部分年报会失败。
> 3. 解析/抽取属重 CPU 任务，已证实轻量（秒级 / MB 级内存），但 9.4 的"Next.js → 队列 → Python Worker"架构仍建议保留以便横向扩容与解耦。

### MVP（技术验证版，收缩范围）— 已定
> 评审后收缩：4–6 周只能完成严格收缩的验证版，不能承诺"完整 MVP + 稳定产品"。

- **范围收缩**：
  - 报告类型：MVP 同时支持**年报、半年报和季报**；招股书进入 P1，并先放入技术测试集。
  - 公司数：**先支持 20–50 家**（种子公司，覆盖三交易所）。
  - 数据源：**原型用东方财富跑通，正式来源用巨潮**（东财仍作兜底，见 7.4）。
  - 指标首版：**仅营收、归母净利润、经营现金流**三项。
  - 引用精度：**摘要 / 问答只要求页码级引用**；段落精确高亮放到 P1。
  - 解析降级：扫描件、复杂合并表首版允许降级（仅阅读 / 低置信度提示）。
  - 终端：**桌面端优先**；移动端保证能用（AI 面板底部抽屉），不做深度优化。
- **账户**：先不登录，匿名可用；进度 / 书签存 localStorage。
- 功能：
  1. 披露列表页（年报 / 半年报 / 季报，F-101~F-104）+ 按公司名称、股票代码查询 + 基础筛选。
  2. PDF 在线阅读（F-201）+ 目录跳转。
  3. AI：一键摘要（F-301）+ 三项指标卡片（F-302 收缩）+ 对话问答（F-305，页码级引用）。
  4. 原文定位（F-202，页码级跳转；段落高亮 P1）。

### P1（增强）
- 招股书全量；段落精确高亮；三大报表可视化（F-303）、术语词典（F-304）、风险提示（F-306）。
- 指标扩充至毛利率 / ROE / 资产负债率等；移动端深度优化；响应式与无障碍打磨。

### P2（增长与商业化）
- 账户体系、关注 / 推送、对比分析（F-307）、导出分享。

> ⚠️ **合规闸门（前置条件，非阶段排期）**：数据正式授权与商业化边界确认（见 7.3）**必须在任何公开测试或商业化上线之前完成**，不归入 P2 排期；当前预研 / 开发可用官方公开接口做技术验证，但不得公开运营。

### 工期估算（参考，待预研后修正）
- 2–3 人团队：稳定产品约 10–16 周。
- 4–6 人且有成熟组件：约 8–12 周。
- 4–6 周：仅限本节的"收缩验证版"，非完整产品。

---

## 11. 风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 官方数据接口变动 / 反爬 | 列表或 PDF 失效 | 多源校验 + 后端代理 + 监控告警 |
| PDF 解析质量差（扫描件、复杂表格） | AI 答非所问 | OCR 兜底；解析失败降级为"仅阅读" |
| AI 幻觉 | 误导用户 | 强制原文引用 + 免责声明 + 人工抽检 |
| 大 PDF 性能 | 卡顿 | 懒加载、虚拟滚动、预抽取缓存 |
| 合规 | 法律风险 | 仅公开数据、来源标注、免责声明 |

---

## 12. 验收标准（技术验证版 MVP）

- [ ] 首页可列出最新**年报、半年报和季报**（覆盖 20–50 家公司、三交易所），并可按报告类型筛选。
- [ ] 支持按**公司名称**或**股票代码**查询；公司全称、简称、模糊名称及带/不带交易所后缀的股票代码均按 F-102 规则返回正确结果。
- [ ] 点击可在线阅读 PDF，目录可跳转。
- [ ] AI 摘要、三项指标卡片（营收 / 归母净利润 / 经营现金流）、问答可用，且回答均带**可点击的页码级引用**。
- [ ] 首页 LCP < 2.5s，PDF 首屏 < 3s，AI 摘要 < 8s（均为 **P95**，测试环境固定见 8.3）。
- [ ] 桌面端体验完整；移动端可用（AI 面板底部抽屉）。
- [ ] 生产环境零控制台错误。
- [ ] 解析 / 抽取 / 引用 / 答案准确率按 **8.2 测量方法** 在测试集上达标：**MVP 阻断指标为三项指标抽取（误差 ≤ 1%、覆盖率 ≥ 95%）+ 页码引用 ≥ 95% + 答案 ≥ 85%**；文本 ≥ 98% 与**通用表格结构 F1 ≥ 85% 移至 P1 / 非阻断**（MVP 复杂表格允许降级，见 8.1）。
- [ ] 预研 5 项（见 10 · 0 阶段）中**未达 Go/No-Go 的部分（连续拉取 / 500+ 页 / 扫描件 / 测试集覆盖率 / embedding+LLM 评测）已在开发期补齐并形成证据**（不再以"概念验证"充当验收）。

---

## 13. 决策状态与待定项

### 已决策（评审收缩后，并入 v0.4 技术基线）
| # | 问题 | 决策 |
| --- | --- | --- |
| 1 | 数据源 | **方案 D：巨潮主 + 东财兜底**；原型用东财跑通，正式源用巨潮（见 7.4 / 7.3） |
| 2 | AI 模型 | **可配置**（OpenAI 兼容协议，多供应商切换，见 9.3） |
| 3 | 招股书 vs 财报 | MVP 支持**年报、半年报和季报**；招股书进技术测试集与 P1（见 10） |
| 4 | 是否登录 | **先不登录**（匿名，localStorage） |
| 5 | 解析准确率基线 | 目标见 8.1；**测量方法见 8.2**（测试集 / 公式 / 验收口径） |
| 6 | 系统架构 | **Next.js 主架构 + 第一版即引入 Python Worker（任务队列 + 对象存储 / 向量库）**，见 9.4 |
| 7 | 工期与范围 | 4–6 周仅够"收缩验证版"；先做 **2 周预研** 再定正式工期（见 10 / 13） |
| 合规 | 数据合规 | 公开 ≠ 可抓取 / 缓存 / 商业化；**授权为前置闸门**：任何公开测试 / 商业化前清 7.3 清单 + 法务复核（不得仅作 P2） |

### 风险评估（评审结论）
- 产品可行性 ≈ 80%，技术可行性 ≈ 75%，**当前排期可信度 ≈ 40–50%（预研仅完成概念验证、未达 Go/No-Go，不应上调）**。
- 主要不确定性：数据获取稳定性（连续拉取未验）、500+ 页 / 扫描件解析（未验）、指标抽取测试集覆盖（仅单份样本）、带引用问答精度（仅 BM25 概念验证）——均须在开发期 / W1–8 补齐验收。

### 待你拍板（仅剩）
- **品牌与域名**：产品名称、域名、Web 优先还是 App（暂不影响预研与 MVP 开发）。

---

> 下一步：**拆分 W1–W2 开发任务（脚手架 + DB + BFF + 首页列表）→ UI 设计并行推进 → 启动开发 → 在 W1–W8 期间补齐剩余预研验收（V1 连续抓取 / V2 500+ 页 + 扫描件 / V4 测试集达标 / V5 embedding + LLM 引用准确率评测）**。
