# W3-5 联调冒烟清单（W3-1 ~ W3-4）

> 对齐：`W3任务拆分.md` 的 W3-5 行。范围：串 W3-1(阅读页骨架) / W3-2(Range 代理) / W3-3(TOC 跳页+高亮脚手架) / W3-4(Worker 管线)。
> 执行：`bash scripts/smoke_w3.sh`（参数 `BASE_URL` / `REPORT_ID` / `USE_DEV_FIXTURE`，见脚本头注释）。
> 沙箱限制：本环境无 Postgres/Redis/运行 worker，且 `next build` 末段清理被 sandbox safe-delete 守卫拦截，
> 故**浏览器实渲染 / 真实 DB 链路 / Lighthouse** 均无法在此终验，留待本地 `docker compose` 环境跑 smoke。

> **2026-07-19 更新：docker 实栈已全绿。** 用户在本地起了 `docker compose`，沙箱现可直连 Postgres(5432)/Redis(6380)。
> 重建 `web`+`worker` 镜像（含最新代码与解析依赖）后，`bash scripts/smoke_w3.sh` 在 `:3001` 实栈跑出 **8/8 PASS**
> （含 PDF Range 代理 200+Accept-Ranges+206 经 dev fixture，及 web→worker 解析触发 200）。详见第五节「实栈验证记录」。

---

## 一、冒烟清单（脚本逐项 PASS/FAIL）

| # | 里程碑 | 端点 / 操作 | 预期 | 说明 |
| --- | --- | --- | --- | --- |
| 1 | W3-1 | `GET /` | 200 HTML | 首页 SSR（标题/标签/列表） |
| 2 | W2/W3 | `GET /api/disclosures` | 200 JSON `{items,total,hasMore}` | 列表；脚本自动取 `items[0].id` 作后续 `REPORT_ID` |
| 3 | W3-3←W3-4 | `GET /api/disclosures/[id]` | 200 JSON 含 `toc[]` | 详情；`toc` 来自 parser 写入（W3-4） |
| 4 | W3-2 | `GET /api/disclosures/[id]/pdf` | 200 + `Accept-Ranges: bytes` | 无 Range 整文件 / 签名 URL 优先 |
| 5 | W3-2 | `GET /api/disclosures/[id]/pdf` `Range: bytes=0-99` | 206 + `Content-Range` | pdf.js 按页懒加载依赖此能力 |
| 6 | W3-4 | `GET /api/disclosures/[id]/parse` | 200 `{status,canParse}` | 前端按钮态 |
| 7 | W3-4 | `POST /api/disclosures/[id]/parse` | 200 / 502 / 409 | 软断言：worker 起→200；未起→502；不可解析(临时/撤回)→409 |

> 假 PDF/TOC 验证：设 `USE_DEV_FIXTURE=1`（服务端须 `FR_ALLOW_DEV_FIXTURE=1`），步骤 4/5 自动追 `?devFixture=sample-report.pdf`，
> 走开发回退分支验证阅读页+代理+跳页，无需真实 PDF 源。

---

## 二、本沙箱内已验证项（非浏览器链路）

- ✅ `apps/web` `tsc --noEmit` 全绿（exit 0）—— 覆盖 W3-1~W3-4 全部前端改动（ReaderPage / PdfViewer / TocPanel / parse 路由 / textLayer.css）。
- ✅ 路由文件齐备：`disclosures/route.ts`、`[id]/route.ts`、`[id]/pdf/route.ts`、`[id]/parse/route.ts`、`reader/[id]/page.tsx`、`page.tsx`。
- ✅ `scripts/smoke_w3.sh` `bash -n` 语法通过；`step` / `step_any` 断言逻辑可正确判定单状态与多状态。
- ✅ W3-4 Worker 单测（SSRF 7/7 + Parser 4/4）在沙箱实跑全绿（见 W3-4 记录）。
- ✅ 502 修复已验证：浏览器等价 Range 请求经 devFixture 走 `206`，worker 干净 URL 走真实代理。

---

## 三、已知问题 / 待补（建议在本地环境确认）

1. **浏览器实渲染未终验**：pdf.js canvas/文本层实际绘制、TOC 点击/键盘跳页、高亮矩形落位，需浏览器手测（沙箱无浏览器）。
2. **`next build` 被 safe-delete 守卫拦末段清理**：产物已生成，仅缺 `prerender-manifest.json`；本地用 `.next.bak` 的 manifest cp 进 `.next` 即可 `next start`。该守卫与 bash 沙箱无关，换环境不受影响。
3. **高亮仅搜已渲染页**：W3-3 脚手架按设计只查 IntersectionObserver 已渲染页（600px 边距）；未加载章节需先滚动触发渲染再查。完整段落级/语义高亮属 W5-6(引用定位)。
4. **Worker 重型依赖本地装**：`requirements-parse.txt`（pymupdf/psycopg2/sentence-transformers 等）沙箱难装，DB 真实写入/幂等需本地 `python -m worker.verify_w3_4.py` 终验。
5. **Lighthouse 基线未跑**（见下）。
6. **3000 端口现有实例为旧构建**：探测到 `:3000` 返回 200 但是 `/api/disclosures` 404（路由未在其实例的构建中或属他项目），**不可作为 W3 冒烟对象**；须用本地 `docker compose` 起的最新构建。

---

## 四、Lighthouse 基线（待浏览器环境补）

本地起最新构建后，对首页 `/` 与阅读页 `/reader/[id]`（devFixture PDF）跑 Lighthouse，记录基线：

| 页面 | 指标 | 目标 | 基线值 | 日期 |
| --- | --- | --- | --- | --- |
| `/` | Performance | ≥90 | ⏳ 待补 | — |
| `/` | Accessibility | ≥90 | ⏳ 待补 | — |
| `/` | Best-Practices / SEO | ≥90 | ⏳ 待补 | — |
| `/reader/[id]` | Performance | ≥90 | ⏳ 待补 | — |
| `/reader/[id]` | Accessibility（键盘/TOC/ARIA） | ≥90 | ⏳ 待补 | — |

> 三栏布局在窄屏降为单栏 + AI 底部抽屉；文本层/高亮均为可选降级，不影响可访问性基线。

---

## 五、实栈验证记录（2026-07-19，docker compose 已起）

### 5.1 运行环境
- `fr_web`(Next 14.2.35) :3001 / `fr_worker`(enqueue:8000 + rq worker) / `fr_postgres`(pg16+pgvector) / `fr_redis` / `fr_minio` 全部 Up & healthy。
- 沙箱经宿主机端口直连 Postgres(5432) / Redis(6380) 成功（容器网络 `worker:8000` 亦通）。

### 5.2 冒烟结果（真实运行实例，:3001）
```
=== W3 联调冒烟 @ http://localhost:3001 ===  USE_DEV_FIXTURE=1
PASS  首页 SSR 200                [/]
PASS  列表 API 200               [/api/disclosures]
PASS  详情 API 200               [/api/disclosures/<id>]
PASS  PDF 代理 200               [/api/disclosures/<id>/pdf?devFixture=sample-report.pdf]
PASS  Accept-Ranges 头存在        (accept-ranges: bytes)
PASS  PDF Range 206              [/api/disclosures/<id>/pdf?devFixture=sample-report.pdf]
PASS  解析状态 GET 200           [/api/disclosures/<id>/parse]
PASS  解析触发 POST 200          [/api/disclosures/<id>/parse]   # web→worker:8000 入队链路打通
汇总 PASS=8 FAIL=0  RESULT: OK
```
- 解析触发 POST 返回 200 ⇒ 证明 **web BFF → worker 内部入队端点(Bearer 鉴权) → RQ 入队** 全链路打通。
- PDF Range 代理 200/206/Accept-Ranges 经 `?devFixture=sample-report.pdf` + `FR_ALLOW_DEV_FIXTURE=1` 验证（开发回退分支，生产不暴露）。

### 5.3 真实 DB 端到端（venv + 可达 Postgres）— W3-4 verify_w3_4.py
- `download(cninfo)` → `parse(3页/3 chunks/2 toc)` → `status=parsed`；幂等、parse_jobs 幂等、主源翻转全绿。
- 修复脚本包导入 bug（PYTHONPATH 指向 `apps` 而非 `apps/worker`）+ 按真实 schema 对齐种子数据。

### 5.4 部署期修复的 4 个缺口（均已合入代码）
1. **config.py 入队端点环境变量名不一致**：原读 `WORKER_ENQUEUE_HOST/PORT`，compose 设 `ENQUEUE_HOST/PORT` ⇒ 回退到 `127.0.0.1:8700`（web 连不上 → parse 502）。改为读 `ENQUEUE_HOST/PORT`，worker 现正确监听 `0.0.0.0:8000`。
2. **compose `worker` 只跑 `rq worker`，漏起 `enqueue_server`** ⇒ web 的解析触发 502。新增 `run_worker.sh` 同时拉起两者，worker 暴露 8000（仅容器网络）。
3. **RQ 任务路径不匹配扁平模块**：原 `worker.pipeline.run_stage`（包式），容器是扁平模块且无 `PYTHONPATH` ⇒ 改为 `pipeline.run_stage` + `PYTHONPATH=/app/apps/worker`。
4. **`Dockerfile.worker` 漏装 `psycopg2`/`pymupdf`** ⇒ download/parse 在容器跑不起。补装（暂不装 torch，留给 W5-6 embed）。

### 5.5 仍待补（非阻塞）
- 浏览器实渲染 / 键盘 TOC / 高亮落位：需浏览器手测（沙箱无浏览器）。
- Lighthouse 基线：需在浏览器环境对 `/` 与 `/reader/[id]` 跑（见第四节）。
- 种子 `pdf_url` 为占位符（非真实可达地址）：经 RQ 真实「下载→解析」会因 URL 不存在而失败，属种子数据问题，非代码缺陷；W3-4 的 download/parse 已用 `local_pdf_path` 直读真实 DB 验证过。

---

## 六、结论

W3 全部子任务（W3-1 ~ W3-4）代码完成，且已在本地 docker 实栈 + 真实 Postgres 完成端到端验证（smoke 8/8、verify_w3_4 全绿、worker 单测 26/26）。
剩余“浏览器实渲染 + Lighthouse 基线”需在浏览器环境补测（沙箱无浏览器），不影响 W3 里程碑交付。
