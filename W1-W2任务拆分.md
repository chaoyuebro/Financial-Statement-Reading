# W1–W2 开发任务拆分（脚手架里程碑）

> 对齐：《技术方案文档.md》v1.0 — §3 单体仓库 / §4 数据模型 / §7.2 BFF 契约 / §7.5 搜索 / §8.1 前端组件树 / §13 里程碑。
> 范围：严格限定"脚手架"里程碑（§13 表第一行）。**年报 / 半年报 / 季报**进入，招股书仅留测试集（P1）。
> W3+ 才涉及：PDF.js 阅读页、Worker 管线、指标/摘要/问答、引用定位、无障碍/性能深度优化。

---

## W1 — 基建与数据层

| # | 任务 | 关键交付 | 验收（可独立判定） |
| --- | --- | --- | --- |
| W1-1 | Monorepo + Next.js 14.2 骨架 | `apps/web`（Next 14.2 App Router + React18 + TS 严格 + Tailwind + 设计 token）、`apps/worker`（Py3.11 占位 + `preresearch/` 归档）、`packages/shared`、`infra/`；pnpm workspace | `pnpm dev` 起空首页无 TS 报错；目录同 §3 |
| W1-2 | docker-compose 编排五服务 | postgres15(+pgvector/pg_trgm 初始化)、redis(RQ)、minio、next、worker；`.env.example` + 一键 up | 五容器健康；next 连通 pg/redis/minio |
| W1-3 | DB 迁移 + 公司种子（§4） | §4 全量 DDL 迁移（含 `chk_rep_period_known`、`uq_disc_src_primary`、`uq_current_disclosure_version`、`vector(512)`+ivfflat、`pg_trgm`）；种子 companies 20–50 家覆盖 sh/sz/bse | migrate 后表/索引齐全；CHECK 对(unknown+NULL)生效；种子可查 |
| W1-4 | packages/shared 契约层 | disclosure 列表/详情/搜索 响应类型 + 常量（type 枚举、状态、排序规则），TS↔Python 同步 | web 与 worker 均 import；跨语言类型一致 |

## W2 — BFF 与首页列表

| # | 任务 | 关键交付 | 验收（可独立判定） |
| --- | --- | --- | --- |
| W2-1 | BFF 列表 API 骨架 `GET /api/disclosures` | 直连 Postgres（暂不接 Worker/向量）；`?type=&exchange=&q=&dateRange=&page=&pageSize=`；仅返回 `is_withdrawn=false AND report_period_unknown=false`；响应 `{items,total,hasMore}` | 种子 curl 通过；筛选/分页正确；列表不含未知期/撤回报告 |
| W2-2 | 名称/代码搜索（§7.5） | 代码去 `.SH/.SZ/.BJ` 后缀精确匹配；name/short_name/former_names/aliases 走 `ILIKE` 前缀 + `pg_trgm` similarity；排序 代码精确>名称精确>简称精确>前缀>模糊 | `600519`/`600519.SH`/`茅台`/`maotai` 均命中且排序合理 |
| W2-3 | 首页列表页 SSR（§8.1） | `HomePage`：TypeTabs + FilterBar + `DisclosureList`(@tanstack/react-virtual) + `DisclosureCard`；语义化 + `role=list`；键盘上下导航 + 回车打开；WCAG 2.1 AA 起步 | 种子 SSR 渲染；键盘可达；无控制台错误 |
| W2-4 | 本地联调冒烟 | 串 W1-1~W2-3；手测筛选/搜索/分页/键盘；跑一次 Lighthouse 记基线；桌面/移动响应式初查 | 冒烟清单全过；记录基线分与已知问题 |

---

## 依赖与执行顺序
```
W1-1 ─► W1-2 ─► W1-3 ─► W1-4
                    │
                    └─► W2-1 ─► W2-2
                              │
                              └─► W2-3 ─► W2-4
```
- W1-3 依赖 W1-1（仓库）+ W1-2（DB 可起）。
- W2-1/W2-2 依赖 W1-3（表与种子）。
- W2-3 依赖 W2-1（API）。
- W2-4 依赖全部。

## 本里程碑明确不做（防范围蔓延）
- ❌ PDF.js 阅读页、目录跳转、引用高亮（W3–4）
- ❌ Worker 下载/解析管线、队列消费（W3–4）
- ❌ 指标抽取 / 摘要 / RAG 问答 / 引用定位（W5–6）
- ❌ 段落级高亮、三大报表可视化、术语词典、招股书全量接入（P1）
- ❌ 准确率 CI gates、并发压测（W7–10）

## 待确认（开工前）
- 包管理器：默认 pnpm（§3 用 pnpm workspace）；若团队用 npm/yarn 请告知。
- 公司种子来源：默认从公开列表手工整理 20–50 家（含 org_id 映射）；是否接 akshare/CNINFO 自动拉取待定（W1-3 内可手动种子）。
- Lighthouse 基线目标：MVP 全程 P95（PRD 8.3），本里程碑只记基线不强制 ≥90。
